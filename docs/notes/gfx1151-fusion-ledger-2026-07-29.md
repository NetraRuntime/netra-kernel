# gfx1151 fusion ledger (2026-07-29)

This ledger distinguishes gfx1151 measured results from untested candidates. MXFP4 weights remain MXFP4 throughout.

| Candidate | Status | gfx1151 evidence | Decision / next gate |
|---|---|---|---|
| GDN QKVZ + BA decode projection | Accepted — gfx1151 measured kernel and serving | Real-checkpoint raw-ASM HIP-event median fell from 0.199833 ms for two dispatches to 0.108543 ms for one padded dispatch, a measured 1.841x speedup. Output is bit-exact to separate raw ASM; staged-FP64 max BF16 absolute error 0.001953125 and normalized L2 2.53e-5. Uncached full-graph 1/+32 median improved 1,944.784 to 1,800.900 ms (1.0799x), and 210/+128 improved 8,033.376 to 7,455.958 ms (1.0774x). | Retained only for M=1. Fixed 1/+32 output hashes match the pre-fusion graph. Prefill keeps separate exact-shape kernels to avoid 448 padded outputs. |
| GDN QKVZ/BA split + reshape + copy | Accepted — gfx1151 measured kernel, serving, and graph | Hand-written raw ASM is bit-exact at M1/M64/M210/M8192 and improves M8192 HIP-event median from 12.437 to 1.772 ms (7.0195x). Uncached 8,192/+1 median improves 7,073.980 to 6,761.489 ms (1.0462x); matched 32,768/+1 improves 35,399.053 to 34,114.981 ms (1.0376x). | Retained for the fixed Qwen3.6 12288/64 layout. M210 serving is neutral. Native M64 `tc_piecewise` capture/replay passes after graph-safe M1 launch boundaries. |
| Decode gate + up + SiLU, N128 | Rejected — measured | Raw ASM fused 193.700 us versus 170.206 us standalone rocprofv3 kernel sum; HIP event 199.508 versus 185.722 us; bit-exact BF16 to standalone; 56 VGPR | 13.8% slower kernel sum and 7.4% slower pipeline. Occupancy loss and four exponential paths outweigh fewer launches/stores. |
| Decode gate + up + SiLU, N64 | Rejected — measured | Raw ASM HIP event 203.829 us; 40 VGPR | Duplicated decode work is slower than both N128 fused and standalone. |
| Prefill M128 reuse | Rejected — measured | Raw ASM 203.129 us versus 197.510 us for two selected M64 calls; 144 VGPR | Register pressure offsets reuse. |
| Prefill gate paired A-tile prefetch | Rejected — gfx1151 measured | Reused post-decode VGPRs with no allocation increase and matched the FP64 result exactly. G=8 measured 105.009 us versus 104.817 us baseline; real G=1,276 measured 11,085.683 us versus 10,658.226 us. | 4.01% slower at the real server shape; extra in-flight traffic reduced rather than hid latency. Standalone raw ASM retained in `kernels/gfx1151/mxfp4/prefill/experiments/`. |
| Prefill gate/up coalesced dword MXFP4 layout | Accepted — gfx1151 measured kernel, counters, graph, and serving | Raw repacker is byte-exact over 134,217,728 bytes. G1276 HIP-event median improves 10.222587 to 8.507615 ms (1.201581x); fetch falls 32.987% and SQ busy cycles 20.161%. Full 32K trace falls 3,456.036 to 2,906.637 ms across 320 calls. Paired 32K serving improves 29,095.883 to 28,530.948 ms with matching greedy tokens. | Retain persistent MXFP4 prefill views. Measured load remains 9.01 s; peak VRAM rises about 10.10 GiB but passes the 49K context capacity gate. |
| Prefill gate four-wave shared activation | Accepted — gfx1151 measured raw ASM, counters, graph, and serving | Four adjacent N16 output waves cooperatively stage one 4 KiB M64xK32 activation tile. Exact G1,276 is bit-exact and improves HIP-event median 8.508117 to 7.174542 ms (1.185876x); fetch falls 27.076%. Full 32K gate GPU time falls 2,856.720 to 2,410.063 ms over 320 calls; fresh serving mean falls 22,078.582 to 21,573.561 ms (1.02341x). | Enabled in production raw gfx1151 ASM. Retain the two barriers; document the 33.195 LDS-bank-conflict metric, lower L2 hit, and +7.834% VALU tradeoffs. Graph replay is bit-exact. |
| Prefill scale-folded BF16 tables | Rejected — measured | HIP event 107.815 us versus 104.992 us baseline; maximum synthetic error 0.001965 versus 0.000327 | Slower and less accurate. |
| Gate + up projection with shared activation loads | Covered by the rejected fused decode experiment | The fused kernel retained 384 `v_perm_b32` and 128 `v_dot2_f32_bf16`, while allocation rose to 56 VGPR | Do not reinstate without a materially different schedule. |
| SiLU x up feeding down projection | Pending; two down reuse subtests rejected | Immutable-weight `glc` measured 0.9919x. Paired-group raw ASM shared weight decode/loads and was byte-exact, but 137 VGPR versus 105 raised latency from 7.34197 to 7.50260 ms (0.9786x). | Reject cache bypass and paired accumulators. Any future fusion must reduce, not add, the live accumulator footprint. |
| Residual + RMSNorm | Pending | Trace inventory captured the family, but it is below the top decode and long-prefill costs | Re-rank after graph launch removal. |
| Q/K norm + RoPE + KV store | Pending | Adjacent kernels are visible in the full-attention sequence; no fused gfx1151 ASM exists | Validate exact RoPE/KV-cache contents before timing. |
| Prefill online-softmax attention | Accepted raw ASM; further cache-policy experiment rejected | Accepted raw kernel and mask fast path remain rank 1. Exact T8192/prefix24576 counters measure 7.377% occupancy, 18.276 GiB fetched, 24.663% L2 hit, and 49.790% LDS bank conflicts. K/V `glc` improved isolated timing 1.0354x and was byte-exact, but changed real-checkpoint pair-b token 96043 to 0. | Keep default load policy. Next raw design must reduce repeated K/V traffic while preserving live KV visibility. |
| Attention Q global-to-LDS and per-tile restore pipeline | Accepted — gfx1151 measured kernel, counters, graph, and serving | Raw qpipe8 is byte-identical, reduces exact T8192 four-tier HIP-event sum 696.698 to 679.227 ms (1.0257x), SQ busy cycles 2.505%, and fetch 5.10%. Two exact 32K/+1 fresh-server pairs improve mean host E2E 24,227.707 to 23,304.863 ms with matching greedy IDs/hashes. Graph replay matches eager. | Superseded in production by group4 shared-KV; retained as the exact oracle. qpipe16 rejected at 0.9982x versus qpipe8. |
| Attention four-query-head shared K/V + register Q overlap | Accepted — gfx1151 measured raw ASM, counters, graph, and serving | Four query heads share one staged K/V tile in a 512-thread workgroup; Q stays global and next fragments overlap current QK. Exact T8192 four-tier HIP-event sum improves 675.593 to 550.197 ms (1.2279x). Full 32K attention time falls 6,791.151 to 5,488.215 ms (1.2374x). Two unprofiled exact 32K/+1 pairs improve mean host E2E 23,304.863 to 22,175.641 ms (1.0509x) with matching IDs/hashes. | Enabled in production raw ASM. Keep the tile-boundary barrier: omitting it caused nondeterministic shared-LDS overwrite corruption. Graph capture/replay is byte-identical at exact T8192. |
| Attention conditional online-softmax output rescale | Rejected — gfx1151 measured raw ASM, rocprofv3, and serving | An exact all-alpha-one ballot skips 128 accumulator multiplies, cuts VALUInsts 878,687.5 to 824,995.5 (6.11%), and improves matched full-trace attention 5,346.822 to 5,325.216 ms. Fresh pair A improves 22,071.103 to 22,000.769 ms, but pair B regresses 22,086.061 to 22,131.321 ms; mean is only 1.000568x. | Restore kvbatch16 production. The end-to-end result is noise-level and not repeatable despite a real dynamic-instruction reduction. |
| Attention softmax exponential issue-distance scheduling | Rejected — gfx1151 measured raw ASM | The candidate issues all eight rows of exponentials before the unchanged sum reductions and is byte-identical at T64 and T8192. Exact T8192 four-tier HIP-event sum is 535.118 versus 534.944 ms (1.000325x); prefix 8K and 24K are neutral/slower. | Keep the interleaved production schedule. The failed HIP-event gate does not justify rocprofv3 or serving runs. Deferred output rescaling remains a distinct untested formulation. |
| Attention 16-page K/V issue batching | Accepted — gfx1151 measured raw ASM, rocprofv3, graph, and serving | One scalar/global wait boundary per K/V prefix phase is removed while preserving all page lookups and the mandatory tile barrier. Exact T8192 prefix tiers are bit-identical and improve four-tier HIP-event sum 548.880 to 535.034 ms (1.02588x); matched full-trace attention improves 5,490.794 to 5,346.822 ms (1.02693x). Two fresh exact 32K/+1 pairs improve mean host E2E 22,144.368 to 22,078.582 ms (1.00298x). | Enabled in production. Keep the measured tradeoff visible: FETCH_SIZE rises 7.896% and L2 hit falls 3.398 points, while MeanOccupancyPerActiveCU rises 3.642%. Graph replay is byte-identical. |
| Attention K/V page-table reuse | Rejected — gfx1151 measured raw ASM and rocprofv3 | Corrected cache8/cache8-first/cache16 variants are byte-identical. Alternating AB/BA exact-T8192 prefix-only sums are 0.99568x, 1.00011x, and 0.99433x versus qpipe8. Full cache cuts static `s_load_b64` sites 33 to 17 and measured mean fetch 9.868%, but regresses prefix-tier HIP-event time 0.567%. | Keep production page lookups. Revisit only with a larger-page/contiguous-page layout that removes broader random K/V traffic. |
| Attention separate Q/P + batched N32 K/V staging | Rejected — gfx1151 measured | Byte-identical raw ASM retained one N64 softmax and reduced LDS 65,536 to 57,344 B. Batched loading recovered a serial prototype's 24-48% loss, but exact T8192 four-tier HIP-event sum was 708.298 versus 697.620 ms (0.9849x). rocprofv3 measured unchanged 8,192 waves and 7.566% versus 7.667% occupancy despite 16.3% lower fetch. | The 56 KiB footprint does not cross a residency threshold; extra phase barriers outweigh cache gains. Do not integrate. |
| Attention V transpose for wide LDS loads | Rejected — gfx1151 measured raw ASM and rocprofv3 | Byte-identical scatter-transpose removed 512 static `ds_load_u16` and 256 V-side swizzles, but added 256 narrow LDS store sites. Exact T8192 four-tier HIP-event sum regressed 678.713 to 1,148.812 ms (0.5908x); LDS-bank-conflict metric rose 34.666 to 74.700. | Keep row-major V. Revisit only if a producer can emit the wide-read layout directly without scatter. |
| GDN state update + gate + normalization | Raw chunk-H still pending; eight-wave compiler oracle rejected | Exact-shape BV32/eight-wave measured 4.5851 ms versus 8.2084 ms and cut scratch 524 to 56 B, but real-checkpoint pair-b changed token 96043 to 3709. | Raw gfx1151 ASM must preserve production accumulation closely enough to pass paired token gates; compiler setting remains four waves. |
| GDN KKT construction + triangular solve | Global FP32 split rejected; exact raw builder retained as experiment; fused raw solve pending | Exact T8192 raw AMDGCN builder reproduces compiler WMMA fragment order with zero FP32/BF16 mismatches and measures 5.308085 versus 5.533179 ms on gfx1151 (1.042406x). Builder + compiler solve is still 9.582621 versus 5.227223 ms fused (1.833214x slower). The LDS builder changes 145 BF16 values; a 91-VGPR remap is exact but slower. | Keep a fused one-wave design. Do not enable the 64 MiB workspace split or either compiler oracle. Reuse the exact raw fragment construction and add raw in-dispatch solve/merge. |
| Router projection + top-k + softmax | Pending | Routing is not yet isolated with counters | Capture expert IDs/weights and preserve exact top-k ties. |
| Expert permutation + activation gather | Accepted — gfx1151 measured | Raw activation pack is bit-equal across all 160 real 32K calls, guard-clean, and replaces 160 fills + 160 gathers + 160 indexed copies. rocprofv3 measures 1,088.123 to 404.818 ms for the segment; paired 32K serving improves 1.0184x and 1.0244x. | Enabled for prefill. Native M64 `tc_piecewise` capture/replay matches eager output. |
| Router-weight application + expert reduction | Accepted — gfx1151 measured kernel, counters, graph, and serving | Deterministic raw ASM fuses padded-row gather, router weighting, top-8 FP64 accumulation, and BF16 conversion. It is bit-exact to the high-precision oracle across all 40 real-checkpoint MoE layers. M8192 HIP events improve 13.008181 to 2.732935 ms (4.759784x); exact 32K reduction GPU time falls 1,130.480012 to 462.722462 ms; paired serving improves 1.095301x. | Enabled for prefill. Keep FP64 fixed-order accumulation: the faster FP32 chain and pair tree changed correctness-sensitive greedy tokens. Residual fusion remains pending. |
| Final RMSNorm + LM head + sampling | Pending | LM-head and sampling are not the first long-prefill target; raw LM-head kernels already exist | Retest after graph capture removes launch overhead. |
| Speculative verify + accept/commit | Blocked by inputs | Supplied checkpoint has no `dflash_config` and no compatible DFlash draft checkpoint is installed | No performance value estimated. |

The accepted QKVZ+BA entry also has a finalized gfx1151 rocprofv3 CSV trace.
Across 221 calls per shape, separate QKVZ and BA raw-ASM kernel medians were
100.351 and 84.922 us, while the fused padded kernel median was 101.114 us: a
measured 1.8323x kernel-time speedup. All three dispatches use 40 VGPR, 128
SGPR, zero LDS, and zero scratch.

The detailed measured negative-result data is also retained in `docs/notes/gfx1151-mxfp4-results.json` and `docs/notes/gfx1151-mxfp4-kernels.md`.
